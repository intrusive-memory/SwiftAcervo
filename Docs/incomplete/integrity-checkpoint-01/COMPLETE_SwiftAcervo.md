---
type: mission-completion-log
state: incomplete
feature_name: OPERATION INTEGRITY CHECKPOINT
mission_branch: mission/integrity-checkpoint/01
iteration: 1
---

# Completed Work — SwiftAcervo (OPERATION INTEGRITY CHECKPOINT)

Generated by Mission Supervisor
Start: 2026-06-28
Last updated: 2026-06-28

## Terminology

> **Mission** — A definable, testable scope of work. Defines scope, acceptance criteria, and dependency structure.

> **Sortie** — An atomic, testable unit of work executed by a single autonomous AI agent in one dispatch. One aircraft, one mission, one return.

> **Work Unit** — A grouping of sorties (package, component, phase).

## Summary
- Total sorties planned: 6
- Total sorties completed: 5 (WU-A complete; WU-B: B1 ✓, B2 ✓; B3 manual E2E pending user decision)
- Models used: opus×2, sonnet×3 (B2 = 1 pass + 1 continuation)
- Total cost: 110x (relative: haiku=1x, sonnet=10x, opus=30x)
- Total duration: ~40 minutes
- WU-A gate: GREEN (consolidated `make test` — main 685/97, tool 98/11, UI 70/12 w/ 2 known issues)
- WU-B gate: GREEN (`make test-unit` 758/83 after B2 continuation)

---

## Work Units

### ✓ Sortie A2: Verified-marker integrity model + `Acervo.verifyIntegrity` + availability fast-path
- **Work Unit**: WU-A SwiftAcervo
- **Status**: COMPLETED
- **Model**: opus (complexity score: 16)
- **Attempts**: 1/3
- **Timing**:
  - Duration: ~7 minutes
  - Turns used: 18 tool uses
- **Git commits**: 19bad01
- **Exit criteria verified**:
  - ✓ `Acervo.verifyIntegrity(_:)` exists, returns `ModelAvailability`, writes `.acervo-verified.json` on passing audit
  - ✓ Matching-checksum marker skips re-hashing (spy: `availabilityEvaluatorOverride` + `HashInvocationSpy`, no timing)
  - ✓ Mismatched marker triggers re-audit (spy shows full-hash path entered)
  - ✓ `verifyIntegrity` on hash-mismatched file returns `.partial`
  - ✓ `make test` green (98 tests, 11 suites — ** TEST SUCCEEDED **)
- **Notes**: Fast-path is full-trust by design (matches plan wording). Marker checksum = `CDNManifest.manifestChecksum`.

---

### ✓ Sortie A1: Harden heuristic verdict for diffusers / multi-folder layout
- **Work Unit**: WU-A SwiftAcervo
- **Status**: COMPLETED
- **Model**: sonnet (complexity score: 7)
- **Attempts**: 1/3
- **Timing**:
  - Duration: ~7 minutes
  - Turns used: 14 tool uses
- **Git commits**: ef16364
- **Exit criteria verified**:
  - ✓ `heuristicVerdict` returns `.indeterminate` (not `.available`) for a diffusers fixture with a missing `transformer/` shard
  - ✓ Non-diffusers fixture (single `config.json`, no shard index) still `.available` (no regression)
  - ✓ New tests in both `EM2ValidityOracleTests.swift` + `AvailabilityThreeStateTests.swift` (confirmed via git: 176 + 109 insertions)
  - ✓ `make test` green (98 tests — ** TEST SUCCEEDED **)
- **Notes**: Diffusers detection = `model_index.json` present AND no root `model.safetensors.index.json`. Only fires when a subdir carries its own shard index (preserves `fluxShape_returnsAvailable`).

---

### ✓ Sortie A3: Write verified marker on download completion
- **Work Unit**: WU-A SwiftAcervo
- **Status**: COMPLETED
- **Model**: sonnet (complexity score: 6)
- **Attempts**: 1/3
- **Timing**:
  - Duration: ~7 minutes
  - Turns used: 27 tool uses
- **Git commits**: 92b3336
- **Exit criteria verified**:
  - ✓ After a successful (mocked) download, `.acervo-verified.json` exists with `manifestChecksum` matching the downloaded manifest
  - ✓ Subsequent `availability` takes the marker fast-path (spy `totalCalls == 0`, oracle never reached)
  - ✓ Partial download (non-empty `requestedFiles`) does NOT write the marker
  - ✓ `make test` green; WU-A consolidated gate green (main 685/97 incl. A2/A3/Diffusers suites)
- **Notes**: Marker write in `AcervoDownloader.downloadFiles` after manifest persistence, full-model only (`requestedFiles.isEmpty`). Reuses A2's `VerifiedMarker.write(in:)`. Best-effort (warn-on-error), matching manifest-persistence pattern.

---

### ✓ Sortie B1: Dependency-aware FLUX components — enumerate the text encoder
- **Work Unit**: WU-B SwiftVinetas
- **Status**: COMPLETED
- **Model**: opus (complexity score: 17)
- **Attempts**: 1/3
- **Timing**:
  - Duration: ~8 minutes
  - Turns used: 30 tool uses
- **Git commits**: db4f59f (SwiftVinetas)
- **Exit criteria verified**:
  - ✓ `modelComponents(for:)` includes `.textEncoder` for klein4B + klein9B
  - ✓ `acervoRepoId` → `lmstudio-community/Qwen3-4B-MLX-8bit` (klein4B) / `Qwen3-8B-MLX-8bit` (klein9B); never routes Mistral (guard test passes)
  - ✓ `availability` returns `.partial(missing:)` when text-encoder repo absent
  - ✓ PixArt regression: all three `componentIds` listed
  - ✓ `make test-unit` green (750 tests/83 suites) — supervisor re-verified with CDN env set
- **Notes**: Introduced SwiftVinetas-owned `Flux2Component` enum w/ nested `TextEncoder` (.klein4B/.klein9B) — plan-sanctioned fallback since upstream `TextEncoderVariant` is Mistral-bound and can't distinguish 4B/9B. SwiftAcervo untouched. DISCOVERY: `make test-unit` requires `ACERVO_CDN_BASE_URL` in runner env (pre-existing gap).

---

### ✓ Sortie B2: Fail-fast generation guard via verifyIntegrity in the load path
- **Work Unit**: WU-B SwiftVinetas
- **Status**: COMPLETED (1 pass + 1 supervisor-directed continuation)
- **Model**: sonnet ×2 (complexity score: 7)
- **Attempts**: 1/3 (first pass PARTIAL → continuation; no failure increment)
- **Git commits**: d09d204 (first pass), 4998dca (continuation)
- **Exit criteria verified**:
  - ✓ `loadModel` guard runs the integrity check per required component **before** the deep loader/pipeline construction
  - ✓ Production default is marker-aware `Acervo.availability(_, verifyHashes: true)` (valid marker → NO re-hash) — corrected from first pass's unconditional `verifyIntegrity` full-hash
  - ✓ `.partial` component → `VinetasError.modelIncomplete(modelID:components:)` up front (message contains "incomplete" + "re-download"); asserted in tests
  - ✓ All-`.available` (marker-valid) path → no throw; checker consulted once per component (new test)
  - ✓ `make test-unit` green (758/83) — supervisor re-verified env requirement
- **Notes**: Supervisor caught that the first pass re-hashed multi-GB models on every generation (defeating the gated-hash thesis) and marked it PARTIAL; continuation switched to A2's marker fast-path. Residual minor: a doc comment overstates that `availability` writes the marker on the no-marker path (it does not — only `verifyIntegrity`/download writes it). No-marker models full-audit each load until a download writes a marker — accepted edge case. Flag for brief.

---

### ⏸ Sortie B3: End-to-end FLUX integrity acceptance — DEFERRED
- **Work Unit**: WU-B SwiftVinetas
- **Status**: DEFERRED (user decision — manual E2E to be run later)
- **Type**: manual
- **Reason**: Needs multi-GB FLUX.2 Klein + Qwen3-MLX-8bit text-encoder + VAE download (not present locally), a GUI mid-download-kill/relaunch via Vinetas.app, and real MLX inference. User chose "Defer B3, close mission now."
- **Coverage in lieu**: The integrity behaviors B3 would confirm end-to-end are already unit-verified — diffusers→`.indeterminate` (A1), marker fast-path (A2/A3), dependency-aware `.partial` + Mistral-trap guard (B1), fail-fast `modelIncomplete` (B2).

---

### ✓ Test Cleanup (post-mission)
- **Status**: COMPLETED
- **Tests removed**: 0 (no mission-added test matched a high-confidence CI-failure pattern)
- **Tests flagged for review**: 0
- **Build verification**: SwiftAcervo `make test` pass; SwiftVinetas `make test-unit` pass
- **Report**: TEST_CLEANUP_REPORT.md
- **Commit**: none (no deletions)

---


